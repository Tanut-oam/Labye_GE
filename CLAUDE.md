# บริบทโปรเจคสำหรับ Claude Code

## โปรเจคนี้คืออะไร

เว็บไซต์กระดานข้อความให้กำลังใจแบบไม่ระบุตัวตน ชื่อ "ล้าบาย"
เป็นโครงงานวิชา GE362785 ของนักศึกษามหาวิทยาลัยขอนแก่น
เป้าหมายคือวัดว่าการใช้เว็บนี้ช่วยลดความเครียดของนักศึกษาได้จริงไหม

## สแตก

HTML + CSS + JavaScript ล้วน ไม่มี build step ไม่มีเฟรมเวิร์ก
ใช้ Firebase (Authentication + Cloud Firestore) ผ่าน ES module จาก CDN
deploy เป็นเว็บ static บน Netlify

ถ้าจะเพิ่มอะไร ให้คงสไตล์นี้ไว้ อย่าเพิ่ม npm package หรือ bundler
เว้นแต่จะคุยกันก่อน

## การไหลของผู้ใช้

สมัครสมาชิก → แบบวัดความเครียด (phase=pre) → กระดาน → โพสต์และคอมเมนต์
→ ครบระยะทดลอง → แบบวัดอีกครั้ง (phase=post) + แบบประเมินความพึงพอใจ

ผู้ใช้เดิมที่เข้าสู่ระบบจะเข้ากระดานตรง ๆ

## ข้อตกลงในโค้ด

- คอมเมนต์และข้อความบนหน้าจอเป็นภาษาไทย ชื่อตัวแปรและฟังก์ชันเป็นอังกฤษ
- สีทุกสีอยู่ใน `assets/css/base.css` เป็น CSS variable อย่าฮาร์ดโค้ดสีในไฟล์อื่น
- ข้อความ error บอกว่าเกิดอะไรและต้องทำอะไรต่อ ไม่ขอโทษ ไม่คลุมเครือ
- ทุกฟอร์มต้องตรวจ input ก่อนส่ง และแสดงข้อความผ่าน `setMsg()` ใน `ui.js`
- หน้าที่ต้องล็อกอินให้เรียก `requireAuth()` จาก `guard.js`

## เรื่องที่ต้องระวังเป็นพิเศษ

กระดานนี้เปิดให้คนระบายความเครียด มีโอกาสเจอข้อความที่ผู้เขียนกำลังเสี่ยง
`moderation.js` จึงแยกเป็นสองชั้น คือบล็อกคำหยาบกับข้อมูลระบุตัวตน
ส่วนข้อความที่ส่อความเสี่ยงต่อตัวผู้เขียนจะไม่บล็อก แต่แสดงช่องทางขอความช่วยเหลือให้
อย่าเปลี่ยนให้บล็อกข้อความกลุ่มหลัง เพราะจะเท่ากับปิดปากคนที่ต้องการความช่วยเหลือที่สุด

เบอร์สายด่วนสุขภาพจิต 1323 ต้องแสดงในหน้ากระดาน หน้าสรุปคะแนน
และตอนที่ระบบตรวจพบข้อความเสี่ยง อย่าย้ายไปซ่อนใน footer

ข้อมูลผู้เข้าร่วมวิจัยห้าม commit ลง repo ไม่ว่ากรณีใด

## สิ่งที่ยังไม่เสร็จ

- `assets/js/data.js` ข้อความแบบวัด 20 ข้อยังเป็นตัวอย่าง ต้องใส่ฉบับจริง
  พร้อมเกณฑ์แปลผลของแบบวัดนั้น ตอนนี้เกณฑ์ 40/60/80 เป็นค่าเดา
- `assets/js/moderation.js` รายการคำยังสั้นมาก ต้องขยาย
- `assets/js/firebase-config.js` ยังเป็นค่าตัวอย่าง
- ยังไม่มีหน้าสำหรับทำแบบวัด post-test ที่เข้าถึงจากกระดาน
- ยังไม่มีสคริปต์ export ข้อมูลไปวิเคราะห์
